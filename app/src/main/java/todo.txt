parser
    value of tile, value of other tiles,
        second of day, second, minute, hour, day, month, year
    to json

buttons ripple
{
    val background = constraintLayout.background as RippleDrawable
    val bgShape = background.findDrawableByLayerId(R.id.fab_shape) as GradientDrawable
    bgShape.color = color
}

screen rotation
tile payload json
publish confirmation
log
notifications
export/import
new tiles
mqttQOS

icon
name
publish