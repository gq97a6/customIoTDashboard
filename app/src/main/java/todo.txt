//expand ------------------------------
start guide
else createToast(adapter.context, "Check setup")
check on receive

new tiles
    keyboard
    trackpad
    lights
        color       | as button, as compound
        brightness  | as seeker, as compound

        mode        | as button
        on/off      | as button

    (indicator,
    diode,
    alert on change,
    on stop receiving,
    on receive)

//post development ------------------------------
tests
name, icon, etc
publish
