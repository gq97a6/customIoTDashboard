package com.netDashboard.tiles.tiles.slider

import com.netDashboard.R
import com.netDashboard.tiles.Tile

class SliderTile(id: Long, name: String, x: Int, y: Int) :
        Tile(id, name, R.layout.slider_tile, x, y)